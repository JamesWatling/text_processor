require 'spec_helper'

describe TextProcessor do
  it 'has a version number' do
    expect(TextProcessor::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
